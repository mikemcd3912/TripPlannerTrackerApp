import 'package:flutter/material.dart';

class BigNavButton extends StatelessWidget {
  const BigNavButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
